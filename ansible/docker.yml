---
- name: Configure Proxmox VMs
  hosts: docker
  vars:
    tfstate_bucket: homelab-tf-state-sylvain
    tfstate_object_key: homelab/pangolin_config.tfstate
    tfstate_s3_endpoint: https://s3.eu-west-par.io.cloud.ovh.net
  pre_tasks:
    # WARNING : You cannot override variables with this, make sure variable is not defined before.
    # Export your age key in environment
    # https://github.com/mozilla/sops/issues/933#issuecomment-1081228720
    - name: Load secrets variable into global namespace
      tags: always
      community.sops.load_vars:
        file: "{{ playbook_dir }}/secrets.sops.yaml"
        expressions: evaluate-on-load

    - name: Read Terraform state from S3 bucket
      delegate_to: localhost
      tags: always
      amazon.aws.s3_object:
        mode: getstr
        bucket: "{{ tfstate_bucket }}"
        object: "{{ tfstate_object_key }}"
        endpoint_url: "{{ tfstate_s3_endpoint }}"
        aws_access_key: "{{ state_s3_access_key }}"
        aws_secret_key: "{{ state_s3_secret_key }}"
      register: terraform_state_raw

    - name: Load healthcheck URLs from Terraform state outputs
      tags: always
      ansible.builtin.set_fact:
        nextcloud_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_nextcloud_url.value | default('') }}"
        wiki_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_wiki_url.value | default('') }}"
        rss_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_rss_url.value | default('') }}"
        monica_v4_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_monica_url.value | default('') }}"
        monica_v4_cron_healthcheck_url: "{{ terraform_outputs.uptime_cron_monica_url.value | default('') }}"
        betisier_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_betisier_url.value | default('') }}"
        meerkat_crm_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_meerkat_crm_url.value | default('') }}"
        flip_planning_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_flip_planning_url.value | default('') }}"
        demo_planning_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_demo_planning_url.value | default('') }}"
        gramps_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_gramps_url.value | default('') }}"
        dawarich_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_dawarich_url.value | default('') }}"
        scanopy_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_scanopy_url.value | default('') }}"
        trek_backup_healthcheck_url: "{{ terraform_outputs.uptime_backup_trek_url.value | default('') }}"
      vars:
        terraform_outputs: "{{ (terraform_state_raw.contents | from_json).outputs | default({}) }}"

  roles:
    - role: geerlingguy.docker
      become: true
      tags: setup
    - role: docker_service
      tags: setup
    - role: host_tuning
      tags: tuning,setup
    - role: borgmatic
      tags: backup,setup
    - role: decommission_app
      tags: [decommission, never]
    - role: newt
      tags: newt,setup
    - role: echo
      tags: echo,app
    - role: rss
      tags: rss,app
    - role: searxng
      tags: searxng,app,search
    - role: betisier
      tags: betisier,app
    - role: monica_v4
      tags: monica,app
    - role: wiki
      tags: wiki,app
    - role: nextcloud
      tags: nextcloud,app
    - role: meerkat_crm
      tags: meerkat_crm,app
    - role: paperless_ngx
      tags: paperless,app
    - role: flip_planning
      tags: flip_planning,app
    # Demo environment: the same role, a second time, with its own compose
    # project, systemd unit, containers, database and Borg repository. Only
    # what cannot be shared is overridden here — image, GHCR credentials,
    # branding assets and mentions légales are inherited from the role defaults
    # and from secrets.sops.yaml, so a change to the application deploys to both.
    #
    # These are role parameters, the highest precedence short of `-e`, so they
    # also win over the flip_planning_* entries of host_vars/docker.
    - role: flip_planning
      tags: demo_planning,app
      vars:
        flip_planning_service: demo_planning
        flip_planning_container_prefix: demo-planning
        flip_planning_domain: demo-planning.sylvain.dev
        flip_planning_sentry_environment: demo
        flip_planning_branding_product_name: "Flip Planning — Démo"
        # Demo traffic has no business in the production audience statistics.
        flip_planning_cloudflare_web_analytics_token: ""
        # Own credentials throughout: a leaked demo password must not open the
        # real planning, and the MCP tokens are bound to the Pangolin resource.
        flip_planning_db_password: "{{ demo_planning_db_password }}"
        flip_planning_pgadmin_password: "{{ demo_planning_pgadmin_password }}"
        flip_planning_admin_password: "{{ demo_planning_admin_password }}"
        flip_planning_session_encryption_key: "{{ demo_planning_session_encryption_key }}"
        flip_planning_mcp_api_key: "{{ demo_planning_mcp_api_key }}"
        flip_planning_mcp_pangolin_access_token_id: "{{ demo_planning_mcp_pangolin_access_token_id }}"
        flip_planning_mcp_pangolin_access_token: "{{ demo_planning_mcp_pangolin_access_token }}"
        flip_planning_backup_enabled: "{{ demo_planning_backup_enabled }}"
        flip_planning_backup_borgmatic_target: "{{ demo_planning_backup_borgmatic_target }}"
        flip_planning_backup_encryption_passphrase: "{{ demo_planning_backup_encryption_passphrase }}"
        flip_planning_backup_healthcheck_url: "{{ demo_planning_backup_healthcheck_url }}"
    - role: gramps
      tags: gramps,app
    - role: dawarich
      tags: dawarich,app
    - role: scanopy
      tags: scanopy,app
    - role: trek
      tags: trek,app
