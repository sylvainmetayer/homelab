---
# https://semaphoreui.com/docs/admin-guide/installation/docker
services:
  postgres:
    container_name: semaphore_postgres
    image: postgres:18@sha256:1a5b3e745bbd82d6deb146505e504da3c2f248cac15e431951b148fbe4f8613a
    hostname: postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER={{ semaphore_db_user }}
      - POSTGRES_PASSWORD={{ semaphore_db_password }}
      - POSTGRES_DB={{ semaphore_db_name }}
    volumes:
      - semaphore-postgres:/var/lib/postgresql/data
    networks:
      - semaphore-internal

  semaphore:
    container_name: semaphore
    image: ghcr.io/sylvainmetayer/homelab/semaphore:v2.18.12@sha256:7fc16c53f07b93cefa77b5c4db206614e816f9795c8f9a9e176c6c11773c0966
    environment:
      - SEMAPHORE_PLAYBOOK_PATH= /tmp/semaphore/
      - SEMAPHORE_DB_DIALECT=postgres
      - SEMAPHORE_DB_HOST=postgres
      - SEMAPHORE_DB_PORT=5432
      - SEMAPHORE_DB_USER={{ semaphore_db_user }}
      - SEMAPHORE_DB_PASS={{ semaphore_db_password }}
      - SEMAPHORE_DB={{ semaphore_db_name }}
      - SEMAPHORE_ADMIN_PASSWORD={{ semaphore_admin_password }}
      - SEMAPHORE_ADMIN_NAME={{ semaphore_admin_name }}
      - SEMAPHORE_ADMIN_EMAIL={{ semaphore_admin_email }}
      - SEMAPHORE_ADMIN={{ semaphore_admin }}
      - SEMAPHORE_ACCESS_KEY_ENCRYPTION={{ semaphore_access_key_encryption }}
      - TZ=Europe/Paris
    restart: unless-stopped
    #volumes:
    #  - ./requirements.txt:/etc/semaphore/requirements.txt
    depends_on:
      - postgres
    networks:
      - newt
      - semaphore-internal

networks:
  newt:
    external: true
  semaphore-internal:

volumes:
  semaphore-postgres:
