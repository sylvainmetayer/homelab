{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "minimumReleaseAge": "1 day",
  "packageRules": [
    {
      "matchCategories": [
        "docker"
      ],
      "pinDigests": true
    },
    {
      "description": "Group all Pangolin role dependencies together",
      "groupName": "pangolin-role-dependencies",
      "matchFileNames": [
        "ansible/roles/pangolin/**",
        "ansible/roles/newt/**"
      ]
    },
    {
      "description": "Pangolin's Docker tags are prefixed with ee- (e.g. ee-1.21.0); teach Renovate to parse them as semver so EE updates are detected",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "docker.io/fosrl/pangolin"
      ],
      "versioning": "regex:^ee-(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$"
    },
    {
      "description": "Group all Immich role dependencies together",
      "groupName": "immich-role-dependencies",
      "matchFileNames": [
        "ansible/roles/immich/**",
        "compose/immich/**"
      ]
    },
    {
      "description": "planning-equipes is our own image: no 1 day cooldown, take updates as soon as they are published. Merging on main triggers the deploy workflow. The custom manager sets depType to the channel (release or main, see the flip_planning role defaults)",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ghcr.io/sylvainmetayer/planning-equipes"
      ],
      "minimumReleaseAge": null,
      "schedule": [
        "at any time"
      ]
    },
    {
      "description": "Production (flip_planning) follows the published releases. semver versioning keeps only a full X.Y.Z: main, latest, sha-xxxxxxx and the floating X.Y tag are ignored",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ghcr.io/sylvainmetayer/planning-equipes"
      ],
      "matchDepTypes": [
        "release"
      ],
      "versioning": "semver",
      "commitMessageTopic": "planning-equipes release (flip_planning)"
    },
    {
      "description": "Patch and minor releases automerge once the lint workflow passes, and so deploy to production",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ghcr.io/sylvainmetayer/planning-equipes"
      ],
      "matchDepTypes": [
        "release"
      ],
      "matchUpdateTypes": [
        "patch",
        "minor"
      ],
      "automerge": true
    },
    {
      "description": "A major release is never deployed on its own (YAML scenario format, environment variables, irreversible Flyway migrations): the PR waits for a manual merge. So does a digest change behind an existing X.Y.Z tag, which should not happen with immutable releases",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ghcr.io/sylvainmetayer/planning-equipes"
      ],
      "matchDepTypes": [
        "release"
      ],
      "matchUpdateTypes": [
        "major",
        "digest"
      ],
      "automerge": false
    },
    {
      "description": "The demo (demo_planning) runs the latest main: every new digest automerges once the lint workflow passes",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ghcr.io/sylvainmetayer/planning-equipes"
      ],
      "matchDepTypes": [
        "main"
      ],
      "matchUpdateTypes": [
        "digest"
      ],
      "commitMessageTopic": "planning-equipes main (demo_planning)",
      "automerge": true
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track the planning-equipes images pinned in the Ansible role defaults (plain vars, no manager covers them); the variable suffix becomes the depType: release (flip_planning) or main (demo_planning)",
      "managerFilePatterns": [
        "/^ansible/roles/flip_planning/defaults/main\\.yml$/"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>\\S+)\\s+depName=(?<depName>\\S+)\\s*\\n\\s*flip_planning_image_(?<depType>release|main):\\s*\"[^\":]+:(?<currentValue>[^\"@]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?\""
      ]
    },
    {
      "customType": "regex",
      "description": "Track Traefik experimental plugin versions in the Pangolin static config (e.g. fosrl/badger); no built-in manager covers Traefik plugins",
      "managerFilePatterns": [
        "/^ansible/roles/pangolin/templates/traefik_config\\.yml\\.j2$/"
      ],
      "matchStrings": [
        "moduleName:\\s*\"github\\.com/(?<depName>[^\"]+)\"\\s*\\n\\s*version:\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "github-releases"
    }
  ]
}
